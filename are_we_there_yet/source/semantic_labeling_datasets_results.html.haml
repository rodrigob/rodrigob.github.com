---
title: Semantic labeling datasets results
---


%div.jumbotron
  %h1 What is the label of each pixel in the image ?

  %p.lead
    Discover the current state of the art in semantic labeling.
  
-# end of header

%div.row.datasets-list
  %ul.span4.offset2.nav.nav-stacked.nav-pills
    - data.datasets["Semantic labeling"].each do |dataset|
      %li= link_to dataset.name, anchor:dataset_anchor(dataset)

- data.datasets["Semantic labeling"].each do |dataset|
  = partial "dataset_results", { :locals => {:dataset => dataset}}
  %hr

    
   


